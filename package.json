{
  "name": "tiny-habit-tracker",
  "version": "1.0.0",
  "description": "A simple habit tracker with streaks",
  "type": "module",
  "scripts": {
    "dev": "npx -y vite",
    "build": "npx -y vite build",
    "preview": "npx -y vite preview"
  }
}
